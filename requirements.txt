# Unified environment for running all five Flower Hub benchmarks from the
# repository root. These versions reconcile conflicting pins in the individual
# application manifests; use the per-application pyproject.toml files when
# creating isolated environments for only one benchmark.

# Federated learning framework and dataset partitioners
flwr[simulation]==1.29.0
flwr-datasets[audio,vision]>=0.6.0
hatchling>=1.26.0

# Shared numerical and ML stack
torch==2.11.0
torchaudio==2.11.0
datasets==4.8.4
huggingface_hub>=0.23.0
numpy==2.2.4
pandas==3.0.1
scikit-learn==1.8.0
tqdm==4.67.3

# Medical image segmentation
monai>=1.3.2
nibabel>=5.2.1
openpyxl>=3.1.0

# Audio tagging
soundfile>=0.12

# Legal LLM / LoRA fine-tuning
transformers==5.6.1
peft==0.19.1
accelerate==1.13.0
sentencepiece>=0.2.0
safetensors>=0.7.0
bitsandbytes>=0.49.2; platform_system != "Darwin"
